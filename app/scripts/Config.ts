// LIVE
export let config = {
    project: {
        version: 'FPeSe6S6hfIyXoVqAA74'
    }
}

// TEST
if (window.location.hostname.search(/test.ts-social-share.dominikangerer.com/i) != -1) {
}

// DEV
if (window.location.hostname.search(/localhost/i) != -1 || window.location.hostname.search(/0.0.0.0/i) != -1) {

}